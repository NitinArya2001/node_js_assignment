let express,APP_PORT,sumArray;_45d‍.w("express",[["default",["express"],function(v){express=v}]]);_45d‍.w("./config",[["APP_PORT",["APP_PORT"],function(v){APP_PORT=v}]]);_45d‍.w("./dataManipulation",[["default",["sumArray"],function(v){sumArray=v}]]);




const app = express();

app.get('/', (req, res) => {
    const numbersArray = [1, 2, 3, 4, 5]; // Sample input array of integers
    try {
      const result = sumArray(numbersArray);
      const responseMessage = `Hello, World!\nSum: ${result}`;
      res.send(responseMessage);
    } catch (error) {
      res.status(400).send(error.message);
    }
});

app.listen(APP_PORT, () => console.log(`Server is running on http://localhost:${APP_PORT}`))